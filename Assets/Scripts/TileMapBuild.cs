using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileMapBuild : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] List<UnityEngine.Tilemaps.Tilemap> tilemap;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
